Python packages to install on RedHat
====================================
In order to properly run the system, the following packages must be installed:

- python3.8
- pip3 install flask
- pip3 install flask-cors 
- pip3 install pyyaml